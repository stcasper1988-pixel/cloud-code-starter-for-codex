# Генерация AGENTS.md под твой проект

**Когда брать:** после заполнения `.business/` через интервью.
**Что делает:** создаёт `AGENTS.md` в корне проекта - главный файл с правилами работы, который Codex читает перед действиями в проекте.
**Результат:** `AGENTS.md` под твой стек и тип проекта, не больше 200 строк.

## Зачем

`.business/` - это **знания** (зачем делаем проект). `AGENTS.md` - это **правила** (как Codex работает со знаниями). Без `AGENTS.md` Codex каждый раз «придумывает» подход заново. С ним - следует единому методу.

⚠️ **Не больше 200 строк** - иначе сжигаешь токены каждый запрос.

## Промпт

```prompt
Создай в корне проекта файл AGENTS.md. Основа - шаблон в templates/AGENTS.md.tmpl.

Действуй так:

1. Прочитай templates/AGENTS.md.tmpl - это заготовка с плейсхолдерами.
2. Прочитай .business/INDEX.md и все файлы внутри .business/ - чтобы понять мой проект.
3. Заполни все плейсхолдеры в шаблоне ({{PROJECT_NAME}}, {{PROJECT_ONE_LINER}}, {{STACK}}, {{LANGUAGE_RULE}}) на основе моих ответов из .business/.
4. Если каких-то данных не хватает - задай мне уточняющие вопросы ПО ОДНОМУ с 3-4 вариантами ответа.
5. Запиши итоговый файл в корень проекта как AGENTS.md.
6. Проверь что файл не больше 200 строк. Если больше - сократи, убирая воду. НЕ удаляй разделы про план для каждой функции и про рефлексию.
7. Покажи мне итоговый файл и спроси что доработать.

Важно: два правила должны остаться в AGENTS.md обязательно:
- «План для каждой новой функции в plans/»
- «Ретроспектива в конце каждой сессии в retrospectives/»

Всегда отвечай на русском.
```

## Проверка

После создания задай Codex микрозадачу: «создай файл HELLO.md с приветствием». Codex должен:
1. Сначала прочитать `AGENTS.md` и `.business/INDEX.md`
2. Создать план в `plans/YYYY-MM-DD-hello.md`
3. Выполнить задачу
4. Обновить план - отметить фазу как `[x]`
5. Написать ретро в `retrospectives/`

Если какой-то шаг пропущен - значит `AGENTS.md` написан плохо. Попроси Codex доработать и повтори тест.
