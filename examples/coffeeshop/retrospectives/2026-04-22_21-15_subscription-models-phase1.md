# Ретро: завершение фазы 1 плана подписки

**Дата:** 2026-04-22 21:15
**Длительность:** ~3 часа
**План:** `plans/2026-04-18-subscription-backend.md`

## 1. Задача

Создать модели данных для подписки Coffee Monthly — таблицы `subscriptions` и `subscription_transactions`, Prisma-схема и миграция.

## 2. Как решал

Сначала прогнал план через критику субагентов (`prompts/methodology/plan-critique.md`). Codex нашёл 2 пробела: не учтена грейс-период при неуспешном продлении + нет защиты от гонки при параллельных заказах. Обновил план.

Потом Codex написал Prisma-схему. После ревью я попросил добавить индекс `(user_id, expires_at)` для быстрой проверки активных подписок. Миграция прошла чисто.

## 3. Решил?

- [x] Да, полностью

## 4. Что можно было лучше

- **Стоило сразу прописать `CREATE INDEX` в плане**, а не вспоминать после генерации схемы. В следующий план заложу индексы как отдельную фазу.
- **Критика через 3 субагента сэкономила ~2 часа переделки на фазе 2-3.** Не пропускаю этот шаг никогда больше для крупных планов.

## 5. Как было / как стало

- **Было:** таблиц под подписку нет, код не знает что такое «активная подписка»
- **Стало:** модели есть, миграция применена, Prisma Client сгенерирован, unit-тест базовых операций прошёл

## Follow-up

- Запланировать работу по API (фаза 2) на 23-24 апреля
- Перечитать Альфа-Банк API про рекуррент **до** фазы 2 — иначе могу написать endpoint не под их формат
